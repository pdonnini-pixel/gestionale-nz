[build]
  base = "Gestionale NZ/frontend"
  command = "echo 'Pre-built dist deployed'"
  publish = "dist"

# Redirect tutte le route a index.html (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
